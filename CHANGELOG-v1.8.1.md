# مَصرفي v1.8.1

إصدار صيانة يركز على سلامة البيانات والخصوصية وعمل التطبيق اليومي.

## الإصلاحات

- عُقمت النصوص التي يكتبها المستخدم قبل عرضها لمنع تشغيل HTML أو JavaScript داخل السجل والحسابات والتحديات.
- صار استيراد النسخة الاحتياطية يفحص كامل الملف قبل تغيير أي بيانات، ويقبل النسخ الفارغة الصحيحة.
- صارت النسخة الاحتياطية تشمل العمليات والحسابات والتحديات والميزانية والخطة.
- رُبط طرفا الحوالة بمعرّف واحد كي يُحذفا معاً، مع دعم الحوالات القديمة قدر الإمكان.
- عند حذف حساب، تُنقل عملياته إلى حساب بديل موجود فعلاً.
- ثُبت يوم التكرار الشهري حتى مع فبراير ونهايات الأشهر، ومُنعت النسخ المكررة.
- استُخدمت التواريخ المحلية بدلاً من UTC، وحُفظت الكسور حتى منزلتين.
- أُصلح التنقل بين الصفحات وحفظ آخر صفحة وظهور الشريط السفلي على الشاشات الواسعة.
- عُزل مخزن Service Worker المؤقت باسم التطبيق، وأصبحت صفحات التنقل تتبع استراتيجية الشبكة أولاً مع بديل أوفلاين.
- أُضيف غلاف Android محلي بلا صلاحية إنترنت أو وصول مباشر إلى ملفات الجهاز، مع منتقي ملفات للاستيراد والتصدير.

## تنبيهات معروفة

- القفل برقم سري هو قفل واجهة محلي ولا يشفّر قاعدة البيانات.
- تغيير العملة يغيّر رمز العرض فقط ولا يحوّل القيم بأسعار صرف.
- ملف APK الحالي موقّع بمفتاح Android debug ومخصص للاختبار المباشر، وليس للنشر في متجر Play.
