# دليل مشروع مَصرفي (Masrofy)

هذا الملف هو المرجع الدائم للمطورين وأدوات البرمجة المساعدة. اقرأه قبل أي تعديل، وحدّثه عندما تتغير بنية المشروع أو قواعد البيانات أو خطوات الإصدار.

## قاعدة مجلد العمل

مجلد `Masrofy` هو النسخة الأساسية والدائمة للمشروع على هذا الجهاز. نفّذ جميع التعديلات القادمة داخله مباشرة، ولا تنشئ مجلد مشروع جديداً باسم الإصدار.

- عدّل الملفات الموجودة داخل `Masrofy` عند كل تحديث.
- أضف سجل التغيير وحدّث أرقام الإصدار في المكان نفسه.
- استبدل ملف APK الموجود في الجذر بالنسخة الجديدة.
- ارفع محتويات هذا المجلد نفسه إلى GitHub.
- ارفع APK في GitHub Releases؛ فهو مستثنى من المستودع بواسطة `.gitignore`.

## 1. تعريف المنتج

مَصرفي تطبيق عربي باتجاه RTL لتسجيل الدخل والمصاريف، إدارة عدة حسابات، التخطيط للميزانية، متابعة تحديات الادخار، وعرض تقارير محلية. يعمل كتطبيق ويب تقدمي PWA وكغلاف Android WebView.

- الواجهة عربية وموجهة أساساً لمستخدمي الجوال في السعودية والمنطقة العربية.
- التطبيق محلي أولاً: لا يحتاج حساب مستخدم أو خادماً لحفظ السجل المالي.
- التحليلات الحالية مبنية على قواعد وحسابات محلية، وليست نموذج ذكاء اصطناعي.
- الإصدار الحالي: `1.9`.
- حزمة Android: `com.masrofy.app`، ورقم البناء الحالي `13`.

## 2. الملفات الأساسية

- `index.html`: هيكل الواجهة والنوافذ والتبويبات.
- `style.css`: التصميم، RTL، الوضع الليلي، الاستجابة وإمكانية الوصول.
- `app.js`: حالة التطبيق، العمليات، الحسابات، الميزانية، التقارير والاستيراد والتصدير.
- `sw.js`: التخزين المؤقت وتشغيل PWA دون اتصال.
- `manifest.json`: معلومات تثبيت PWA والأيقونات.
- `chart.min.js`: Chart.js محلي حتى تعمل الرسوم دون شبكة.
- `checks.cjs`: اختبارات منطق البيانات والأمان.
- `server.cjs`: خادم محلي بسيط للتجربة.
- `android-src/`: مصدر غلاف Android وسكربت بناء APK.
- `CHANGELOG-v*.md`: سجل تغييرات كل إصدار.

ملفات الويب في جذر المشروع هي المصدر الأساسي. ينسخ `android-src/build-debug.ps1` هذه الملفات إلى أصول Android وقت البناء؛ لا تعدّل النسخ المولدة داخل `android-src/assets` أو `android-src/build`.

## 3. نموذج البيانات

تحفظ الحالة في `localStorage` تحت المفتاح `masrofy-v1`. عدم تغيير اسم المفتاح مقصود للحفاظ على بيانات مستخدمي الإصدارات السابقة.

العملية المالية تحتوي عادة على:

- `id`: رقم فريد.
- `type`: إما `income` أو `expense`.
- `amount`: رقم موجب مع دعم منزلتين عشريتين في العرض.
- `category`: معرف من قائمة الفئات المعتمدة لنوع العملية.
- `date`: تاريخ محلي بصيغة `YYYY-MM-DD`.
- `note`: نص اختياري لا يزيد على 80 حرفاً.
- `accountId`: معرف حساب موجود.
- `recur`: `none` أو `daily` أو `weekly` أو `monthly`.
- `anchorDay` و`nextDate`: يستخدمان للعمليات المتكررة.
- `seriesId`: يربط النسخ الناتجة بقالب التكرار.
- `transfer` و`transferId`: يربطان طرفي الحوالة بين الحسابات.

صيغة النسخة الاحتياطية الحالية هي `version: 3` وتشمل العمليات والحسابات والتحديات والميزانية والخطة. لا تصدر الرقم السري للقفل.

## 4. قواعد سلامة البيانات

هذه القواعد إلزامية في كل تعديل:

1. يجب أن يبقى حساب واحد على الأقل.
2. يجب أن تشير كل عملية إلى حساب موجود.
3. الحوالة عمليتان متعاكستان تشتركان في `transferId`. حذفها أو استعادتها يشمل الطرفين، ولا تدخل في الدخل أو المصروف الحقيقي عبر `REAL()`.
4. الاستيراد ذري: افحص الملف كاملاً قبل تغيير `state`. عند وجود خطأ، تبقى بيانات المستخدم كما هي.
5. لا تقبل مبلغاً غير محدود أو غير موجب، أو تاريخاً غير صالح، أو فئة خارج القائمة المعتمدة.
6. التكرار الشهري يحافظ على `anchorDay` حتى مع فبراير ونهايات الأشهر، ويمنع تكرار النسخة نفسها باستخدام السلسلة والتاريخ.
7. الميزانية تخص الشهر الحالي وتشمل الالتزامات الثابتة والمصروف المتغير عند إنشائها من المعالج.
8. بطاقات البداية تعرض دخل ومصروف وصافي الشهر الحالي. مجموع الأرصدة الكامل يظهر في قسم الحسابات.
9. تغيير العملة يغير رمز دفتر الحساب فقط ولا يحول القيم. يجب إبلاغ المستخدم بذلك.
10. لا تستبدل البيانات ببيانات تجريبية أو تمسحها دون تأكيد واضح، ووفّر التراجع عندما يكون ذلك عملياً.

## 5. الأمان والخصوصية

- لا تضف تحليلات أو اتصالات خارجية أو تتبعاً أو خطوطاً أو مكتبات CDN دون حاجة واضحة وموافقة صاحب المشروع.
- لا تدمج نص المستخدم مباشرة في `innerHTML`. استخدم `textContent` أو `escapeHtml` مع قائمة قيم مسموح بها، واربط الأحداث عبر `addEventListener` في العناصر الديناميكية.
- افحص كل قيمة مستوردة، حتى لو جاءت من نسخة احتياطية أنشأها التطبيق.
- حافظ على حماية CSV من الصيغ التي تبدأ بـ `=`, `+`, `-`, أو `@`.
- القفل الحالي قفل واجهة محلي ولا يشفّر `localStorage`. لا تصفه كتشفير أو حماية بنكية.
- لا تسجل المبالغ أو الملاحظات أو النسخ الاحتياطية في console أو خدمات خارجية.
- غلاف Android لا يطلب صلاحية الإنترنت، ويمنع وصول WebView المباشر إلى ملفات ومحتوى الجهاز، ويستخدم أصلاً محلياً باسم `appassets.androidplatform.net`.
- الروابط الخارجية تفتح في المتصفح الخارجي، والاستيراد والتصدير يستخدمان منتقي ملفات Android.

## 6. قواعد تجربة المستخدم

- حافظ على العربية وRTL والمصطلحات البسيطة.
- صمم للجوال أولاً، ثم تحقق من عرض سطح المكتب.
- يجب أن يبقى شريط التنقل متاحاً في جميع الصفحات.
- استخدم `page-hidden` لإخفاء صفحة غير نشطة، و`hidden` للحالة الداخلية المشروطة مثل بطاقة خطة غير موجودة أو إشعار تحديث. لا تخلط بينهما.
- حافظ على حد لمس مناسب، وتسميات `aria-label`، وحالة تركيز مرئية، واحترام `prefers-reduced-motion`.
- اذكر فترة كل رقم بوضوح: اليوم، هذا الشهر، الشهر المختار، أو الإجمالي.
- لا تستخدم كلمة «آمن» لوصف مبلغ محسوب من الميزانية فقط؛ استخدم «المتاح يومياً».
- اعرض التجاوز والعجز كما هما، ولا تقص النسب عند 100% أو 0%.

## 7. PWA والعمل دون اتصال

- غيّر اسم `CACHE` في `sw.js` عند كل إصدار منشور.
- احذف فقط المخازن التي تبدأ بالبادئة `masrofy-`.
- استخدم بديل `index.html` لطلبات التنقل فقط، ولا ترجعه عند فشل JavaScript أو صورة.
- خزّن استجابات الأصل نفسه بعد التأكد من نجاحها.
- لا تطبق Service Worker جديداً بالقوة أثناء استخدام الصفحة. اعرض إشعار التحديث، ثم نفذ `SKIP_WAITING` بعد اختيار المستخدم.
- اختبر PWA عبر HTTPS أو `localhost`. فتح `index.html` باستخدام `file://` لا يختبر Service Worker.

## 8. Android

يتطلب البناء Android SDK مع platform وbuild-tools للإصدار 34 وJava متوافقاً.

```powershell
.\android-src\build-debug.ps1
```

الناتج الحالي: `Masrofy-v1.9-debug.apk`.

- نسخة debug للاختبار والمشاركة المباشرة فقط.
- للنشر في Google Play أنشئ مفتاح release دائم، احفظه بأمان، وارفع `versionCode`.
- قبل تغيير اسم الحزمة أو التوقيع، خطط لترحيل بيانات المستخدم؛ Android قد يعتبرها تطبيقاً مختلفاً أو يرفض التحديث.

## 9. الاختبار المحلي

نفذ هذه الفحوص بعد تعديل المنطق:

```powershell
node --check app.js
node --check sw.js
node checks.cjs
```

لتجربة الواجهة:

```powershell
node server.cjs
```

ثم افتح `http://localhost:8765` واختبر على عرض جوال وسطح مكتب.

يجب أن تغطي المراجعة اليدوية على الأقل:

- إضافة وتعديل وحذف ثم التراجع عن عملية.
- دخل ومصروف بمبلغ عشري.
- حوالة بين حسابين وثبات مجموع الأرصدة.
- حذف حساب مستخدم ونقل عملياته إلى حساب صحيح.
- تصدير نسخة احتياطية ثم استيرادها ومطابقة الأرصدة والخطة والتحديات.
- تكرار شهري في يوم 31 وفبراير والسنة الكبيسة.
- اختيار شهر التقرير، ونسب الميزانية والعجز، والرصيد الافتتاحي للرسم.
- التنقل وإعادة التحميل وحفظ آخر تبويب.
- تشغيل PWA دون شبكة بعد أول تحميل، ثم تدفق تحديث Service Worker.
- استيراد وتصدير ملف من APK على جهاز Android فعلي.

## 10. خطوات إصدار جديد

1. حدّث رقم الإصدار في تعليق `app.js` وسجل التغييرات وREADME.
2. غيّر اسم كاش PWA في `sw.js`.
3. حدّث `versionName` وارفع `versionCode` في `android-src/AndroidManifest.xml`.
4. حدّث اسم APK الناتج في `android-src/build-debug.ps1`.
5. نفذ اختبارات Node والفحص اليدوي للواجهة.
6. ابنِ APK وتحقق من التوقيع واسم الحزمة والإصدار والصلاحيات.
7. تأكد أن ملفات الويب داخل APK تطابق ملفات جذر المشروع.
8. ارفع المصدر إلى الفرع الرئيسي، وارفع APK التجريبي في GitHub Releases بدلاً من وضعه داخل المستودع.
9. لا ترفع `android-src/build` أو `android-src/assets` أو ملفات `*.apk` و`*.idsig` إلى المستودع؛ يغطيها `.gitignore`.

## 11. قيود معروفة وأولويات قادمة

- البيانات غير مشفرة داخل `localStorage`.
- العملة واحدة لدفتر الحساب وتغييرها لا يحول الأرقام.
- لا توجد مزامنة بين الأجهزة أو استرداد سحابي.
- APK الحالي تجريبي ويحتاج توقيع release واختبار جهاز فعلي قبل النشر العام.
- الأولويات القادمة: رصيد افتتاحي مستقل، أرشفة الحسابات، تحديد حدود الفئات دون معالج كامل، تحسين الوصول، واختبارات تلقائية للواجهة.

عند تعارض اقتراح جديد مع سلامة البيانات أو الخصوصية، قدم سلامة السجل المالي أولاً ووضح أثر القرار في `CHANGELOG`.
